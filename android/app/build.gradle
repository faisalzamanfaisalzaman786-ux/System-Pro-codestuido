apply plugin: 'com.android.application'

android {
    namespace 'com.systempro.app' // یہ خود بخود YAML سے اپڈیٹ ہو جائے گا
    compileSdk 34

    defaultConfig {
        applicationId "com.systempro.app" // یہ بھی خود بخود YAML سے اپڈیٹ ہو جائے گا
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // Basic Android Components
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    implementation 'androidx.activity:activity:1.8.0'
    
    // Networking & JSON (Required for your GitHub API calls)
    implementation 'com.squareup.retrofit2:retrofit:2.9.0'
    implementation 'com.squareup.retrofit2:converter-gson:2.9.0'
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    
    // Image Processing (Required for your Icon Handling)
    implementation 'com.github.bumptech.glide:glide:4.16.0'
    
    // Animation (Required for Lottie if used)
    implementation 'com.airbnb.android:lottie:6.1.0'
    
    // Test Libraries
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}
