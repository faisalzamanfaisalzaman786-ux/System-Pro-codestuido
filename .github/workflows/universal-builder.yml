name: System Pro Remote Builder (Ultra Clean)

on:
  repository_dispatch:
    types: [build-app]

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Deep Clean Environment
        # ریپوزٹری کو کلین اسٹیٹ میں لانا تاکہ پرانا کوئی ڈیٹا رنر پر باقی نہ رہے
        uses: actions/checkout@v4
        with:
          clean: true

      - name: 2. Purge Previous Artifacts & Temporary Files
        run: |
          echo "🧹 Starting Deep Clean..."
          # اینڈرائیڈ روٹ اور عارضی فائلوں کی مکمل صفائی
          rm -rf android/
          rm -rf app/src/main/
          rm -rf *.xml *.java *.apk
          # گریڈل کے پرانے کیشے اور ورکنگ ڈائریکٹریز کی صفائی
          rm -rf ~/.gradle/caches/
          rm -rf ~/.gradle/wrapper/
          echo "✨ Environment is now Crystal Clean."

      - name: 3. Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 4. Build Fresh Project Structure
        run: |
          APP_NAME="${{ github.event.client_payload.app_name || 'SystemProApp' }}"
          PKG_NAME="${{ github.event.client_payload.package_name || 'com.systempro.app' }}"
          PKG_PATH=$(echo "$PKG_NAME" | tr '.' '/')
          ANDROID_ROOT="android"
          
          # نئے سرے سے فولڈرز بنانا (کوئی پرانی فائل یہاں نہیں بچ سکے گی)
          mkdir -p $ANDROID_ROOT/app/src/main/java/$PKG_PATH
          mkdir -p $ANDROID_ROOT/app/src/main/res/layout
          mkdir -p $ANDROID_ROOT/app/src/main/res/values
          mkdir -p $ANDROID_ROOT/app/src/main/res/mipmap-xxhdpi

          echo "Injecting Fresh Code Assets..."
          echo '${{ github.event.client_payload.java }}' > $ANDROID_ROOT/app/src/main/java/$PKG_PATH/MainActivity.java
          sed -i "1s|^package .*|package $PKG_NAME;|" $ANDROID_ROOT/app/src/main/java/$PKG_PATH/MainActivity.java

          echo '${{ github.event.client_payload.xml }}' > $ANDROID_ROOT/app/src/main/res/layout/activity_main.xml

          cat <<EOF > $ANDROID_ROOT/app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET" />
              <application
                  android:allowBackup="true"
                  android:label="@string/app_name"
                  android:icon="@mipmap/ic_launcher"
                  android:roundIcon="@mipmap/ic_launcher"
                  android:theme="@style/Theme.App">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF
          
          if [ ! -z "${{ github.event.client_payload.icon_data }}" ]; then
            echo "${{ github.event.client_payload.icon_data }}" | base64 -d > $ANDROID_ROOT/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
          fi
          
          # ریسورسز جنریشن
          echo '<?xml version="1.0" encoding="utf-8"?><resources><style name="Theme.App" parent="Theme.MaterialComponents.DayNight.NoActionBar" /></resources>' > $ANDROID_ROOT/app/src/main/res/values/styles.xml
          echo '<?xml version="1.0" encoding="utf-8"?><resources><color name="colorPrimary">#00FFCC</color><color name="colorPrimaryDark">#000000</color></resources>' > $ANDROID_ROOT/app/src/main/res/values/colors.xml
          echo "<?xml version=\"1.0\" encoding=\"utf-8\"?><resources><string name=\"app_name\">$APP_NAME</string></resources>" > $ANDROID_ROOT/app/src/main/res/values/strings.xml

      - name: 5. Fresh Gradle Initialization
        run: |
          PKG_NAME="${{ github.event.client_payload.package_name || 'com.systempro.app' }}"
          ANDROID_ROOT="android"
          cd $ANDROID_ROOT
          
          # آن لائن تازہ ترین گریڈل ریپر ڈاؤن لوڈ کرنا
          gradle wrapper --gradle-version 8.1.1
          
          echo "android.useAndroidX=true" > gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties
          
          cat <<EOF > settings.gradle
          pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
              repositories { google(); mavenCentral() }
          }
          rootProject.name = "SystemPro"
          include ':app'
          EOF

          cat <<EOF > build.gradle
          buildscript {
              repositories { google(); mavenCentral() }
              dependencies { classpath 'com.android.tools.build:gradle:8.1.1' }
          }
          EOF

          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application' }
          android {
              namespace "$PKG_NAME"
              compileSdk 34
              defaultConfig {
                  applicationId "$PKG_NAME"
                  minSdk 24
                  targetSdk 34
                  versionCode ${{ github.run_number }}
                  versionName "1.0"
              }
              compileOptions {
                  sourceCompatibility JavaVersion.VERSION_17
                  targetCompatibility JavaVersion.VERSION_17
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'com.google.android.material:material:1.9.0'
              implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
          }
          EOF

      - name: 6. Execute Clean Build
        run: |
          cd android
          chmod +x gradlew
          # 'clean' کمانڈ کے ساتھ بلڈ چلانا تاکہ پرانا کوئی بھی ڈیٹا مکس نہ ہو
          ./gradlew clean assembleDebug --no-daemon

      - name: 7. Post-Build Cleanup & Upload
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: SystemPro-Clean-Build-${{ github.run_number }}
          path: android/app/build/outputs/apk/debug/*.apk

      - name: 8. Final Waste Removal
        if: always()
        run: |
          # بلڈ ختم ہونے کے بعد تمام حساس اور عارضی ڈیٹا ڈیلیٹ کرنا
          echo "🚮 Finalizing and cleaning workspace..."
          rm -rf android/
          rm -rf temp_layout.xml
