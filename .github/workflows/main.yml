      - name: 🧬 AI Guardian: Icon & Package Master
        shell: python
        run: |
          import os, json, re, base64

          # پے لوڈ حاصل کرنا
          event_path = os.getenv('GITHUB_EVENT_PATH')
          with open(event_path, 'r') as f:
              event_data = json.load(f)
          payload = event_data.get('client_payload', {})

          # 1. Global Package Name Sync
          # اگر پے لوڈ میں پیکج نیم ہے تو وہ استعمال ہوگا، ورنہ ڈیفالٹ
          target_pkg = payload.get('target_package', 'com.system.pro').strip()
          pkg_path = target_pkg.replace('.', '/')

          # 2. Icon Processing (Base64 to Image)
          icon_data = payload.get('icon_base64')
          if icon_data:
              try:
                  img_data = base64.b64decode(icon_data)
                  icon_dirs = ['mipmap-mdpi', 'mipmap-hdpi', 'mipmap-xhdpi', 'mipmap-xxhdpi']
                  for d in icon_dirs:
                      path = f'app/src/main/res/{d}'
                      os.makedirs(path, exist_ok=True)
                      with open(f'{path}/ic_launcher.png', 'wb') as f:
                          f.write(img_data)
                  print(f"AI Success: App icon processed and placed in all resource folders.")
              except Exception as e:
                  print(f"AI Warning: Icon processing failed: {e}")

          # 3. Java Code & Package Fixer
          raw_java = payload.get('java_code', "")
          # AI Logic: پے لوڈ والے پیکج نیم کو جاوا کوڈ میں زبردستی سیٹ کرنا
          if raw_java:
              java_purified = re.sub(r'^package\s+[\w\.]+;', f'package {target_pkg};', raw_java.strip())
              # بریکٹ بیلنسنگ
              open_b = java_purified.count('{')
              close_b = java_purified.count('}')
              if open_b > close_b: java_purified += "\n" + ("}" * (open_b - close_b))
          else:
              java_purified = f"package {target_pkg};\nimport android.os.Bundle;\nimport androidx.appcompat.app.AppCompatActivity;\npublic class MainActivity extends AppCompatActivity {{ @Override protected void onCreate(Bundle savedInstanceState) {{ super.onCreate(savedInstanceState); setContentView(R.layout.activity_main); }} }}"

          # 4. XML Layout Cleanup
          xml_purified = re.sub(r'^[^<]+', '', payload.get('xml_code', "").strip())
          if not xml_purified.startswith('<?xml'): xml_purified = '<?xml version="1.0" encoding="utf-8"?>\n' + xml_purified

          # فائلیں لکھنا (متحرک راستوں کے ساتھ)
          java_dir = f'app/src/main/java/{pkg_path}'
          os.makedirs(java_dir, exist_ok=True)
          os.makedirs('app/src/main/res/layout', exist_ok=True)
          
          with open(f'{java_dir}/MainActivity.java', 'w', encoding='utf-8') as f:
              f.write(java_purified)
          with open('app/src/main/res/layout/activity_main.xml', 'w', encoding='utf-8') as f:
              f.write(xml_purified)

          # پیکیج نیم کو انوائرنمنٹ میں محفوظ کرنا تاکہ گریڈل استعمال کر سکے
          with open('env_vars.sh', 'w') as f:
              f.write(f"export TARGET_PKG='{target_pkg}'")

      - name: 📝 Sync Build Configurations
        run: |
          source env_vars.sh
          # App build.gradle میں پیکیج نیم اپ ڈیٹ کرنا
          cat <<EOF > app/build.gradle
          plugins { id 'com.android.application' }
          android {
              namespace '$TARGET_PKG'
              compileSdk 33
              defaultConfig {
                  applicationId "$TARGET_PKG"
                  minSdk 24
                  targetSdk 33
                  versionCode 1
                  versionName "1.0"
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'com.google.android.material:material:1.9.0'
          }
          EOF

          # مینی فیسٹ میں آٹو پیکج سنک
          cat <<EOF > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application android:label="System Pro" android:icon="@mipmap/ic_launcher" android:theme="@style/Theme.AppCompat">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF
