name: System Pro UI Trigger

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'بغیر کسی تبدیلی کے چلانے کے لیے'
        required: false
        default: 'true'

jobs:
  ui-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checking UI Trigger
        run: echo "UI Trigger is working!"
