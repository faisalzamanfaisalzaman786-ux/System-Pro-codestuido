      - name: 🚀 4. Smart Build
        id: build_step
        run: |
          gradle wrapper --gradle-version 8.5
          chmod +x gradlew
          # اب ہم ایک خاص کمانڈ استعمال کریں گے جو APK کو صحیح جگہ بنائے گی
          ./gradlew :app:assembleDebug --no-daemon
