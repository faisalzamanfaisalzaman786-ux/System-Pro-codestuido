name: Build APK

on:
  push:
    branches: [ "main" ]
  # یہ لائن مینوئل چلانے کے لیے ضروری ہے
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # باقی کوڈ...
